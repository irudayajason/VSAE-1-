# Core dependencies
torch>=2.0.0
transformers>=4.30.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Hindsight SDK for ablation history tracking
hindsight-client>=0.1.0

# Optional: for better performance
accelerate>=0.20.0